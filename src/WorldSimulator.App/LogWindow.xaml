<Window x:Class="WorldSimulator.App.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Журнал симуляции"
        Width="900"
        Height="600"
        MinWidth="700"
        MinHeight="400">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Технический журнал"
                   FontWeight="SemiBold"
                   FontSize="18"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1"
                   Text="Показываются последние 500 записей."
                   Foreground="DimGray"
                   Margin="0,0,0,10"/>

        <ListBox x:Name="TechnicalLogList"
                 Grid.Row="2"
                 ItemsSource="{Binding TechnicalLogEntries}"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
    </Grid>
</Window>
