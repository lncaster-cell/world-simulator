<UserControl x:Class="WorldSimulator.App.Views.SelectedCityPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WorldSimulator.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel, IsDesignTimeCreatable=False}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/MainWindowResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border BorderBrush="LightGray"
                         BorderThickness="1"
                         CornerRadius="6"
                         Padding="12"
                         Margin="0,0,0,12"
                         Visibility="Visible">
                    <StackPanel>
                        <TextBlock Text="Выбранный город" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding SelectedCity.SelectedCityName}" FontSize="18" FontWeight="Bold" Margin="0,0,0,8"/>
                        <TextBlock Text="Тип/профиль:" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding SelectedCity.SelectedCityProfile}" TextWrapping="Wrap" Margin="0,0,0,8"/>

                        <TextBlock Text="Состояние города:" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding SelectedCity.CityStateDisplay}" Margin="0,0,0,6"/>

                        <TextBlock Text="Население:" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding SelectedCity.Population}" Margin="0,0,0,6"/>

                        <TextBlock Text="Ресурсы:" FontWeight="SemiBold" ToolTip="{Binding SelectedCity.ResourcesTooltip}"/>
                        <TextBlock Text="{Binding SelectedCity.ResourcesDisplay}" ToolTip="{Binding SelectedCity.ResourcesTooltip}" Margin="0,0,0,6"/>

                        <TextBlock Text="Товары:" FontWeight="SemiBold" ToolTip="{Binding SelectedCity.GoodsTooltip}"/>
                        <TextBlock Text="{Binding SelectedCity.GoodsDisplay}" ToolTip="{Binding SelectedCity.GoodsTooltip}" Margin="0,0,0,6"/>

                        <TextBlock Text="Пища:" FontWeight="SemiBold" ToolTip="{Binding SelectedCity.FoodBalanceTooltip}"/>
                        <TextBlock Text="{Binding SelectedCity.FoodDisplay}" ToolTip="{Binding SelectedCity.FoodBalanceTooltip}" Margin="0,0,0,6"/>

                        <TextBlock Text="Безопасность:" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding SelectedCity.Security}" Margin="0,0,0,6"/>

                        <TextBlock Text="Преступность:" FontWeight="SemiBold" ToolTip="{Binding SelectedCity.CrimeFlowTooltip}"/>
                        <TextBlock Text="{Binding SelectedCity.Crime}" ToolTip="{Binding SelectedCity.CrimeFlowTooltip}" Margin="0,0,0,6"/>

                        <TextBlock Text="Настроение:" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding SelectedCity.Mood}" Margin="0,0,0,12"/>

                        <WrapPanel>
                            <Button Content="Открыть город" Width="130" Command="{Binding OpenSelectedCityCommand}" Click="OpenCityButton_Click" Margin="0,0,8,0"/>
                            <Button Content="Открыть летопись города" Width="140" Click="OpenLogButton_Click"/>
                        </WrapPanel>
                    </StackPanel>
                </Border>

</UserControl>